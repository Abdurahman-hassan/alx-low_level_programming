# include <stdio.h>

/**
  * main - Entry point
  * 
  * Return: Always 0 (Success)
  */

int main(void)
{
	char msg[] = "with proper grammar, but the outcome is a piece of art,";
	puts(msg);
	return (0);
}
